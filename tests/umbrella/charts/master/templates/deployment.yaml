{{- $config := include "core.general.config" . | fromYaml }}
{{- $context := merge (dict "$" $) $config }}
kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ include "core.deployment.name" $context }}
  labels: {{ include "core.deployment.labels" $context | nindent 4 }}
spec:
  replicas: {{ include "core.deployment.replicas" $context }}
  selector:
    matchLabels: {{ include "core.common.labels" $context | nindent 6 }}
  revisionHistoryLimit: {{ ternary 1 (int $context.revisionHistoryLimit) (kindIs "invalid" $context.revisionHistoryLimit) }}
  strategy: {{ include "core.common.strategy.rollingUpdate" $context | nindent 4 }}
  template:
    metadata:
      labels: {{ include "core.common.labels" $context | nindent 8 }}
      {{- $annotations := include "core.pod.annotations" $context }}
      {{- if $annotations }}
      annotations: {{ $annotations | indent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ $context.serviceAccount | default "default" }}
      {{- $imagePullSecrets := include "core.pod.imagePullSecrets" $context }}
      {{- if $imagePullSecrets }}
      imagePullSecrets: {{ $imagePullSecrets | indent 6 }}
      {{- end }}
      {{- $volumes := include "core.pod.volumes" $context }}
      {{- if $volumes }}
      volumes: {{ $volumes | indent 6 }}
      {{- end }}
      {{- $initContainers := include "core.pod.initContainers" $context }}
      {{- if $initContainers }}
      initContainers: {{ $initContainers | indent 6 }}
      {{- end }}
      terminationGracePeriodSeconds: {{ ternary 30 (int $context.terminationGracePeriodSeconds) (kindIs "invalid" $context.terminationGracePeriodSeconds) }}
      topologySpreadConstraints: {{ include "core.pod.topologySpreadConstraints" $context | nindent 6 }}
      containers: 
        {{- include "core.container.render" $context | indent 8 }}
        {{- with (include "core.container.sidecars" $context) }}
        {{- . | indent 8 }}
        {{- end }}
